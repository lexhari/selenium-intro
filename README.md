# Selenium Introduction
This project is a basic setup for web automation using **Selenium** and **Microsoft Edge**.

## Setup Requirements
* Python 3.14.2
* Selenium Library
* Microsoft Edge Browser
* EdgeDriver (compatible with your Edge version)

## How to Run
The main script is \`intro.py\`. Ensure your EdgeDriver is configured in your system path or specified in the script." > README.md